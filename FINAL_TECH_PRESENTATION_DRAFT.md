# 가자GO 최종 기술 발표 구성안

## 발표 방향

기존 발표는 개발환경과 API 개념 설명의 비중이 컸다. 최종 발표에서는 개발환경 설명을 최소화하고, 실제 앱에서 동작하는 구현 결과를 중심으로 구성하는 것이 좋다.

핵심 메시지는 다음과 같다.

- 가자GO는 방한 외국인을 위한 한국 여행 보조 앱이다.
- 단순 정보 제공이 아니라, 실제 API/DB/AI를 연결해 사용자의 현재 상황에 맞는 실행 가능한 결과를 제공한다.
- TMAP POI와 대중교통 API를 통해 장소와 경로를 실제 데이터로 검증한다.
- Gemini는 교통 챗봇에서 자연어를 구조화하고, 경로 후보를 사용자의 조건에 맞게 비교하는 역할을 한다.
- 행사/음식/일정/저장 기능은 RDS DB와 JWT 인증을 기반으로 사용자별 데이터로 연결된다.

## 추천 목차

1. 프로젝트 개요
2. 기존 서비스 분석
3. 시스템 설계
4. 핵심 API 연동 구조
5. 주요 기능 구현
6. 구현 결과
7. 기대 효과
8. 한계점 및 향후 개선 방향

기존 요청 목차 기준으로는 아래 4번부터 10번을 중심으로 발표하면 된다.

## 기존 기술발표 대비 최종 구현 보강 내용

기존 기술발표는 Expo, Spring Boot, FastAPI, TMAP POI, Gemini 챗봇의 구조와 설계 방향을 설명하는 데 초점이 있었다. 최종 발표에서는 “어떤 기술을 선택했는가”보다 “선택한 기술이 실제 앱 기능으로 어떻게 완성되었는가”를 강조하면 좋다.

### 1. FE 개발환경 설명에서 최종 앱 구조 설명으로 전환

기존 발표 내용:

- Expo React Native
- TypeScript
- NativeWind
- 앱 화면 흐름도

최종 구현 보강:

- 화면 단위 컴포넌트와 API 호출 로직을 분리했다.
- `src/app/api`에서 백엔드 API 요청을 담당하고, `src/app/hooks`에서 화면 상태와 로딩/에러 상태를 관리한다.
- 교통, 음식, 행사, 편의시설, 일정 API 응답은 프론트에서 normalize 과정을 거쳐 화면에서 안정적으로 사용할 수 있게 했다.
- 더미 데이터 기반 화면을 실제 API 데이터 기반 화면으로 교체했다.

발표 문장:

> 초기 발표에서는 Expo와 TypeScript를 중심으로 개발환경을 설명했지만, 최종 구현에서는 화면 컴포넌트와 API 연결 계층을 분리하는 구조로 발전시켰습니다. 특히 교통, 음식, 행사, 일정 API 응답을 화면에서 바로 쓰지 않고 정규화하는 계층을 두어 백엔드 응답 변화에도 안정적으로 대응할 수 있게 했습니다.

발표에 넣을 키워드:

- API layer
- Custom hook
- Normalize
- Loading/Error state
- 더미 데이터 제거

### 2. 백엔드 개발환경 설명에서 서비스 API 완성으로 전환

기존 발표 내용:

- Spring Boot
- FastAPI
- MySQL RDS
- Docker/AWS

최종 구현 보강:

- Spring Boot는 회원 인증, 행사, 음식, 편의시설, 일정, 저장 API를 담당한다.
- FastAPI는 교통 챗봇의 AI 처리 서버로 분리했다.
- RDS에는 회원, 행사, 음식, 알레르기, 교통 요청, 경로 요약, 저장 행사, 일정 데이터가 연결된다.
- Docker Compose로 Spring Boot와 FastAPI 서버를 함께 실행해 API 연동 테스트를 진행했다.

발표 문장:

> 기존 발표에서는 백엔드 기술 스택을 소개하는 수준이었다면, 최종 구현에서는 Spring Boot와 FastAPI의 역할을 명확히 분리했습니다. Spring Boot는 서비스 API와 인증을 담당하고, FastAPI는 Gemini와 TMAP 대중교통 처리를 담당하도록 구성했습니다.

추가 설명:

```text
Spring Boot
  - 회원/JWT 인증
  - 행사/음식/편의시설/일정 API
  - RDS 엔티티 관리

FastAPI
  - Gemini 자연어 분석
  - TMAP 대중교통 API 호출
  - 경로 후보 요약 및 추천 처리
```

### 3. TMAP POI 개념에서 실제 기능 적용으로 확장

기존 발표 내용:

- POI API 필요 이유
- 장소 검증 과정
- POI API 요청 구조

최종 구현 보강:

- 교통 챗봇에서 출발지/목적지 후보 검증에 POI를 적용했다.
- 편의시설 화면에서도 같은 TMAP POI 기반으로 은행, ATM, 편의점, 화장실을 조회한다.
- 현재 위치 기준 반경 3km, 최초 10개, 추가 5개씩 조회하는 방식으로 API 과부하를 줄였다.
- POI 응답에서 도로명 주소, 지번 주소, 위도, 경도, 거리 정보를 정규화했다.
- Google Maps 외부 링크는 좌표 기반으로 열리게 하여 검색어 정확도 문제를 줄였다.

발표 문장:

> 기존 발표에서 POI는 교통 챗봇의 장소 검증용으로 설명했지만, 최종 구현에서는 편의시설 기능까지 확장했습니다. 즉 TMAP POI를 한 기능에만 쓰지 않고, 장소 후보 검증과 현재 위치 주변 시설 검색에 함께 재사용했습니다.

추가로 강조할 점:

- DB를 새로 만들지 않고 외부 POI API 재사용
- 좌표 기반 조회로 정확도 확보
- 페이지네이션으로 요청량 제어
- 장소명 검색이 아니라 실제 위치 데이터 기반

### 4. 대중교통 API 설계에서 실제 경로 추천 화면으로 완성

기존 발표 내용:

- 대중교통 경로 후보 조회
- 경로 후보를 비교 가능한 특징으로 변환
- 외부 API 예외 처리

최종 구현 보강:

- 사용자가 선택한 POI 좌표를 기준으로 TMAP 대중교통 API를 호출한다.
- 경로 후보는 최대 3개를 받아온다.
- 각 경로는 소요 시간, 환승 수, 도보 거리, 요금, 상세 이동 단계로 정규화된다.
- 프론트에서는 경로 카드 형태로 비교하고, 선택한 경로는 타임라인 형태로 상세 표시한다.
- 선택한 경로는 최근 경로로 임시 저장되고, 일정 저장까지 연결된다.

발표 문장:

> 최종 구현에서는 TMAP 대중교통 API 응답을 그대로 화면에 표시하지 않고, 사용자가 비교할 수 있는 경로 요약 정보로 변환했습니다. 이 정보를 기반으로 경로 카드, 상세 타임라인, 최근 경로 저장, 일정 저장까지 이어지도록 구현했습니다.

화면 설명:

```text
추천 경로 카드
  - 추천 순위
  - 소요 시간
  - 환승 수
  - 요금
  - 도보 거리
  - 상대적 장점 태그

선택 경로 상세
  - 도보 / 버스 / 지하철 단계
  - 단계별 이동 시간과 거리
  - Google Maps 경로 열기
  - 오늘 일정 저장
```

### 5. Gemini 챗봇 설명에서 실제 AI 처리 플로우로 보강

기존 발표 내용:

- Structured Output
- Function Calling
- 맞춤형 추천
- 경로 판단
- 프롬프트 설계 예시

최종 구현 보강:

- Gemini는 두 번 사용된다.
- 첫 번째는 사용자 문장에서 출발지, 목적지, 제약 조건을 추출하는 단계다.
- 두 번째는 TMAP 경로 후보 3개를 비교해 우선순위를 정하는 단계다.
- Gemini가 직접 장소나 경로를 만들어내는 것이 아니라, TMAP 실데이터를 기반으로 판단하도록 설계했다.
- JSON 응답을 요구해 Spring Boot와 FE가 안정적으로 파싱할 수 있게 했다.

발표 문장:

> Gemini는 최종 답변을 임의로 생성하는 챗봇이 아니라, 교통 요청을 구조화하고 TMAP 실데이터를 해석하는 역할로 제한했습니다. 이 구조 덕분에 AI의 자연어 이해 능력과 외부 API의 정확한 위치/교통 데이터를 함께 사용할 수 있었습니다.

Gemini 적용 구조:

```text
1차 Gemini
사용자 문장 -> 출발지 / 목적지 / 제약 조건 JSON 추출

2차 Gemini
TMAP 경로 후보 3개 -> 사용자 조건 기준 우선순위 판단
```

### 6. DB 저장 구조에서 사용자 개인화 기능으로 보강

기존 발표 내용:

- 추천 결과 DB 저장 구조
- 요청과 후보 경로 분리 저장
- 이력 관리 가능성

최종 구현 보강:

- 교통 요청은 `user_request`에 저장된다.
- 추천 경로 요약은 `route_summary`에 저장된다.
- 새로 `saved_festival`, `schedule_item` 구조를 추가해 사용자별 저장 행사와 일정을 관리한다.
- 캘린더와 내 일정은 더미 데이터가 아니라 로그인 사용자 기준 DB 데이터를 조회한다.
- 행사 저장, 일정 등록, 일정 수정/삭제가 가능하다.

발표 문장:

> 기존 발표에서는 교통 요청과 경로 저장 구조를 중심으로 설명했지만, 최종 구현에서는 개인 일정 관리까지 확장했습니다. 사용자가 저장한 행사와 직접 작성한 일정은 JWT 인증 사용자 기준으로 DB에 저장되고, 캘린더와 내 일정 화면에서 다시 조회됩니다.

사용자 데이터 흐름:

```text
JWT 로그인
  -> 사용자 식별
  -> 저장 행사 조회
  -> 월별 일정 조회
  -> 날짜별 메모/행사/교통 일정 관리
```

### 7. 음식 기능의 최종 보강

기존 발표에는 음식 기능이 핵심 기술 발표 내용으로 크게 다뤄지지 않았지만, 최종 앱에서는 실제 DB 연동 기능으로 보강되었다.

최종 구현 내용:

- 사용자가 피해야 하는 알레르기 성분을 선택한다.
- FE가 `selectedAllergies` 배열을 서버에 전송한다.
- 백엔드는 선택 성분이 포함된 음식을 제외하고 안전 음식 목록을 반환한다.
- `FoodResponse` DTO를 확장해 음식명뿐 아니라 성분 목록도 함께 반환한다.
- FE는 음식 카드에서 성분을 간결하게 표시한다.

발표 문장:

> 음식 기능은 최종 구현에서 DB 기반 필터링 기능으로 보강했습니다. 단순히 음식명을 보여주는 것이 아니라, 사용자가 선택한 알레르기 성분을 기준으로 서버에서 음식을 필터링하고 각 음식의 성분 정보까지 함께 제공합니다.

### 8. 행사 기능의 최종 보강

기존 발표에서는 행사 기능보다 교통 기술 설명이 중심이었지만, 최종 앱에서는 행사 DB 연동과 저장 기능이 핵심 사용자 흐름으로 들어갔다.

최종 구현 내용:

- RDS `festival_info` 데이터를 기반으로 행사 목록을 조회한다.
- 행사 상세 화면에서 주소, 기간, 이미지, 전화번호, 요금, 홈페이지, 개요를 표시한다.
- 지역별 필터와 진행 상태 필터를 적용했다.
- 진행 상태는 전체, 진행 중, 곧 시작, 종료 임박으로 구분한다.
- 저장한 행사는 마이페이지에서 확인하고 캘린더 일정으로 등록할 수 있다.

발표 문장:

> 행사 기능은 최종 앱에서 사용자의 여행 계획 시작점 역할을 합니다. 행사 목록을 보고 끝나는 것이 아니라, 관심 행사를 저장하고 캘린더에 등록해 개인 일정으로 이어지도록 구현했습니다.

### 9. 최종 구현에서 추가로 해결한 문제

최종 개발 과정에서 단순 기능 구현 외에도 실제 앱 실행을 위해 다음 문제들을 해결했다.

- FE와 BA API 엔드포인트 불일치 수정
- JWT 인증 헤더 누락 문제 확인 및 유지
- Expo LAN 테스트 환경에서 IP/base URL 정리
- Docker 기반 Spring Boot/FastAPI 서버 실행 확인
- TMAP API 키와 Gemini API 키 환경변수 이름 정리
- RDS 연결 확인
- 행사/음식/일정 더미 데이터 제거
- 음식 성분 응답 DTO 확장
- 편의시설 TMAP POI 카테고리/주소/거리 정규화
- 교통 경로 결과에서 불필요한 일반 문구 제거
- UX 관점에서 삭제 버튼, 상세 경로, 외부 지도 연결 등 개선

발표 문장:

> 최종 구현 과정에서는 기능을 새로 추가하는 것뿐 아니라, 실제 앱 실행 중 발생한 API 주소, 인증, 환경변수, 네트워크, 데이터 형식 문제를 하나씩 정리했습니다. 이 과정을 통해 화면만 있는 앱이 아니라 서버와 외부 API가 연결된 동작 가능한 앱으로 완성했습니다.

## 최종 결과 보강용 PPT 슬라이드 추가안

기존 PPT 뒤쪽의 “전체 구현화” 장표를 아래 내용으로 바꾸거나 추가하면 좋다.

### 추가 슬라이드 A. 기존 설계 대비 최종 구현

| 기존 발표 | 최종 구현 |
| --- | --- |
| 기술 스택 소개 | 실제 API 계층과 화면 상태 관리 구현 |
| POI 장소 검증 설계 | 교통 후보 검증 + 편의시설 주변 검색 적용 |
| Gemini 챗봇 구조 | 자연어 분석 + 경로 후보 우선순위 판단 구현 |
| RDS 저장 구조 | 행사 저장, 일정, 메모, 경로 저장 흐름 확장 |
| 앱 화면 흐름도 | 로그인 후 행사/음식/편의시설/교통/일정 실제 동작 |

### 추가 슬라이드 B. 최종 교통 챗봇 구현 결과

- 자연어 질문 입력
- Gemini로 출발지/목적지/조건 추출
- TMAP POI 후보 제공
- 사용자가 정확한 장소 선택
- TMAP 대중교통 API로 경로 후보 3개 조회
- Gemini가 조건 기반 우선순위 판단
- 경로 카드와 상세 타임라인 표시
- 최근 경로 및 일정 저장 연결

### 추가 슬라이드 C. 최종 데이터 연동 결과

- 행사: RDS DB 목록/상세 조회
- 음식: 알레르기 조건 기반 DB 필터링
- 편의시설: TMAP POI 실시간 조회
- 교통: Gemini + TMAP + RDS 저장
- 일정: JWT 사용자별 저장/조회/수정/삭제

### 추가 슬라이드 D. 최종 앱 완성도 보강

- 더미 데이터 제거
- API 연결 상태 확인
- JWT 인증 유지
- Docker 서버 실행 검증
- Expo 앱 테스트
- 외부 지도 연결
- 사용자별 저장 데이터 확인

## 4. 기존 서비스 분석

### 기존 서비스의 한계

기존 여행/지도 서비스는 각각의 기능은 강하지만, 방한 외국인의 실제 이동 흐름을 한 화면에서 이어주지는 못한다.

- 지도 앱: 장소 검색과 길찾기는 강하지만, 사용자의 여행 맥락이나 제약 조건을 자연어로 반영하기 어렵다.
- 관광 정보 앱: 축제/행사 정보는 제공하지만, 저장한 행사와 개인 일정, 이동 경로가 자연스럽게 연결되지 않는다.
- 음식 정보 앱: 음식 추천은 가능하지만, 한국 음식의 알레르기 성분을 사용자가 선택한 조건 기준으로 바로 필터링하기 어렵다.
- 일반 챗봇: 자연어 응답은 가능하지만, 실제 POI/대중교통 데이터와 연결되지 않으면 잘못된 장소나 경로를 말할 위험이 있다.

### 가자GO의 차별점

가자GO는 여행자가 한국에서 바로 써야 하는 기능을 하나의 앱 흐름으로 연결한다.

- 행사 탐색: DB 기반 행사 목록/상세 조회
- 교통 챗봇: Gemini 자연어 분석 + TMAP POI 검증 + 대중교통 경로 추천
- 편의시설: 현재 위치 기반 TMAP POI 주변 검색
- 음식: 알레르기 선택 기반 안전 음식 필터링
- 일정: 행사 저장, 캘린더 등록, 메모 저장

발표 문장 예시:

> 기존 서비스가 각각의 정보를 따로 제공했다면, 가자GO는 방한 외국인이 실제 여행 중 겪는 "어디를 가고, 무엇을 먹고, 어떻게 이동하고, 무엇을 저장할지"를 하나의 앱 안에서 연결하는 것을 목표로 했습니다.

## 5. 시스템 설계

### 전체 구조

가자GO는 프론트엔드, Spring Boot 서버, FastAPI AI 서버, AWS RDS, 외부 API로 구성된다.

- FE: Expo React Native 기반 모바일 앱
- Spring Boot: 인증, 행사, 음식, 편의시설, 일정 저장 API 제공
- FastAPI: Gemini 기반 교통 자연어 분석 및 경로 추천 처리
- AWS RDS MySQL: 회원, 행사, 음식, 알레르기, 교통 요청, 경로 요약, 저장 행사, 일정 데이터 저장
- TMAP API: POI 장소 검색, 주변 편의시설 검색, 대중교통 경로 조회
- Gemini API: 사용자 문장 분석, 경로 후보 비교 및 추천 우선순위 생성

### 최종 앱 데이터 흐름

```text
React Native App
  -> Spring Boot API
    -> JWT 인증 / 사용자 식별
    -> RDS 데이터 조회 및 저장
    -> TMAP POI/대중교통 API 호출
    -> FastAPI 교통 AI 서버 호출
      -> Gemini 자연어 분석 및 경로 추천
```

### 인증과 사용자 데이터 연결

로그인 성공 시 JWT 토큰을 발급받고, 프론트엔드는 이후 인증이 필요한 API 요청에 토큰을 포함한다.

사용자별로 연결되는 데이터는 다음과 같다.

- 저장한 행사 목록
- 캘린더/내 일정 메모
- 교통 챗봇 요청 및 경로 요약
- 향후 확장 가능한 사용자 선호 정보

## 6. 개발 환경

최종 발표에서는 개발환경을 길게 설명하지 않는 것이 좋다. 한 장으로 압축한다.

### 최소 설명

- Frontend: Expo React Native, TypeScript
- Backend: Spring Boot, FastAPI
- Database: AWS RDS MySQL
- External API: TMAP API, Gemini API
- Infra: Docker Compose 기반 서버 실행

발표 문장 예시:

> 개발환경 자체보다 중요한 부분은 각 기술이 맡은 역할입니다. React Native는 사용자 화면과 모바일 테스트를 담당하고, Spring Boot는 서비스 API와 인증을 담당하며, FastAPI는 Gemini와 TMAP 대중교통 연산을 분리 처리하도록 설계했습니다.

## 7. 주요 기능 구현

## 7-1. 교통 챗봇 구현

### 기능 목표

사용자가 자연어로 이동 요청을 입력하면, 앱이 출발지/목적지를 추출하고 실제 장소 후보를 보여준 뒤, 선택한 장소 기준으로 대중교통 경로를 추천한다.

### 구현 흐름

```text
1. 사용자가 자연어 입력
   예: "서울역에서 경복궁까지 다리를 다쳤는데 어떻게 가?"

2. Spring Boot /api/transport/InitialRequest 호출

3. FastAPI가 Gemini로 문장 분석
   - departure
   - destination
   - constraints

4. Spring Boot가 TMAP POI API로 장소 후보 검증
   - 출발지 후보 5개
   - 목적지 후보 5개

5. 사용자가 정확한 출발지/목적지 선택

6. Spring Boot /api/transport/routes 호출

7. FastAPI가 TMAP 대중교통 API로 경로 후보 3개 조회

8. Gemini가 사용자 조건을 반영해 경로 우선순위와 추천 사유 생성

9. 경로 요약과 상세 이동 단계를 앱에 표시
```

### 구현 포인트

- 자연어 입력을 바로 길찾기에 쓰지 않고, Gemini로 구조화한 뒤 TMAP POI로 검증한다.
- 장소명을 단일 결과로 확정하지 않고 후보군을 보여줘 잘못된 위치 선택을 줄인다.
- 경로 후보는 시간, 환승 수, 도보 거리, 요금, 상세 이동 단계로 정규화한다.
- 최근 선택 경로를 프론트에서 임시 저장해 사용자가 다시 확인할 수 있게 했다.
- 상세 경로에서는 도보/버스/지하철 단계를 타임라인 형태로 보여준다.
- 선택 경로는 Google Maps 경로 링크와 일정 저장 기능으로 이어진다.

### 발표 강조 문장

> 챗봇이 단순히 문장으로 답하는 것이 아니라, Gemini가 사용자의 의도를 구조화하고 TMAP이 실제 장소와 경로를 검증하는 구조입니다. 따라서 AI 응답의 자유도는 유지하면서도 실제 이동 가능한 결과를 제공할 수 있습니다.

### 기술 구현 상세

교통 챗봇은 프론트에서 한 번에 모든 처리를 하지 않고, FE, Spring Boot, FastAPI, Gemini, TMAP이 역할을 나누는 방식으로 구현했다.

#### 1단계: 프론트 입력 처리

관련 파일:

- `src/app/screens/TransportScreen.tsx`
- `src/app/hooks/useTransportChat.ts`
- `src/app/api/transport.ts`

구현 방식:

- 사용자가 채팅 입력창에 자연어 문장을 입력한다.
- `useTransportChat` 훅이 메시지 상태, 장소 후보 상태, 선택된 경로 상태를 관리한다.
- API 호출은 `createTransportInitialRequest(message)` 함수로 분리했다.
- 응답 형식이 `departurecandidates`, `departureCandidates`, `departure_candidates`처럼 달라질 수 있어 프론트에서 normalize 함수를 두어 안정적으로 변환했다.

발표용 설명:

> 프론트에서는 챗봇 화면의 상태 관리를 `useTransportChat` 훅으로 분리했습니다. 사용자의 입력, 장소 후보, 경로 목록, 선택 경로, 로딩 상태를 한 곳에서 관리하고, API 응답은 normalize 함수를 통해 화면에서 쓰기 쉬운 형태로 변환했습니다.

#### 2단계: Spring Boot 교통 API 진입

관련 파일:

- `ChatController.java`
- `ChatService.java`

API:

```text
POST /api/transport/InitialRequest
POST /api/transport/routes
```

구현 방식:

- `/InitialRequest`는 사용자의 첫 메시지를 받는다.
- Spring Boot는 JWT 인증 정보를 통해 로그인 사용자를 확인한다.
- 이후 FastAPI 서버에 메시지를 전달해 Gemini 분석 결과를 받는다.
- 분석된 출발지/목적지를 TMAP POI API로 검색해 후보 리스트를 만든다.

발표용 설명:

> Spring Boot는 교통 챗봇의 메인 게이트웨이 역할을 합니다. 사용자의 로그인 정보를 확인하고, 자연어 분석은 FastAPI로 넘기며, 실제 장소 후보 검증은 TMAP POI API를 통해 수행합니다.

#### 3단계: FastAPI + Gemini 자연어 분석

관련 파일:

- `gazagopy/transit_chatbot.py`

Gemini 분석 결과:

```json
{
  "departure": "서울역",
  "destination": "경복궁",
  "constraints": "다리를 다침"
}
```

구현 방식:

- FastAPI는 Spring Boot에서 받은 사용자 문장을 Gemini에 전달한다.
- 프롬프트에서 반드시 JSON만 반환하도록 제한한다.
- 추출 대상은 `departure`, `destination`, `constraints` 세 가지다.
- 이 결과는 Spring Boot로 돌아가고, 이후 POI 검색 키워드로 사용된다.

발표용 설명:

> Gemini는 최종 경로를 바로 말하는 역할이 아니라, 사용자의 문장을 서버가 처리 가능한 구조로 바꾸는 역할을 합니다. 이 구조화 단계가 있어야 TMAP API에 정확한 장소 검색 요청을 보낼 수 있습니다.

#### 4단계: TMAP POI 후보 검증

관련 파일:

- `TmapClient.java`
- `LocationCandidateResponse.java`

구현 방식:

- Gemini가 추출한 장소명은 그대로 확정하지 않는다.
- TMAP POI API에 `searchKeyword`로 전달한다.
- 출발지 후보 5개, 목적지 후보 5개를 반환한다.
- 각 후보는 이름, 주소, 위도, 경도를 포함한다.
- 사용자가 후보 중 하나를 직접 선택하도록 하여 장소 오인식을 줄였다.

발표용 설명:

> AI가 추출한 장소명을 바로 확정하지 않고, TMAP POI로 실제 존재하는 장소 후보를 다시 검증했습니다. 이를 통해 챗봇의 자연어 이해와 실제 지도 데이터 사이의 불일치를 줄였습니다.

#### 5단계: 선택 좌표 기반 대중교통 경로 생성

관련 파일:

- `ChatService.java`
- `transit_chatbot.py`
- `RouteSummary.java`
- `RouteSummaryRepository.java`

구현 방식:

- 사용자가 선택한 출발지/목적지 좌표를 `user_request` 테이블에 저장한다.
- FastAPI는 `request_id`를 기준으로 DB에서 좌표와 사용자 조건을 조회한다.
- TMAP 대중교통 API에 출발/도착 좌표를 전달한다.
- 응답에서 경로 후보 3개를 추출한다.
- 각 경로는 소요 시간, 환승 수, 도보 거리, 요금, 상세 이동 단계로 요약된다.

발표용 설명:

> 경로 생성 단계에서는 사용자가 직접 선택한 좌표를 기준으로 TMAP 대중교통 API를 호출합니다. 외부 API 응답은 그대로 쓰지 않고, 앱에서 비교 가능한 형태의 경로 요약 DTO로 변환했습니다.

#### 6단계: Gemini 경로 우선순위 판단

Gemini가 비교하는 값:

- 총 소요 시간
- 환승 횟수
- 도보 거리
- 요금
- 사용자 제약 조건
- 경로 상세 단계

구현 방식:

- TMAP에서 받은 3개 경로 요약을 Gemini에 다시 전달한다.
- Gemini는 각 경로의 `rank`와 `reason`을 JSON으로 반환한다.
- 서버는 경로 요약과 추천 결과를 `route_summary` 테이블에 저장한다.
- FE는 저장된 경로 요약을 받아 카드 형태로 표시한다.

발표용 설명:

> TMAP은 실제 경로 후보를 제공하고, Gemini는 사용자의 상황을 고려해 후보를 비교합니다. 예를 들어 짐이 많거나 다리를 다쳤다는 조건이 있으면 도보 거리와 환승 수가 추천 판단에 더 큰 영향을 주도록 설계했습니다.

#### 7단계: 프론트 경로 결과 표시

관련 파일:

- `RouteList.tsx`
- `RouteDetail.tsx`
- `RecentRoutePanel.tsx`
- `transportHistory.ts`

구현 방식:

- 경로 3개를 카드로 보여준다.
- 각 카드에는 소요 시간, 환승 수, 요금, 도보 거리, 상대적 장점 태그를 표시한다.
- 사용자가 경로를 선택하면 상세 이동 단계가 타임라인 형태로 펼쳐진다.
- 최근 선택한 경로는 로컬에 임시 저장해 다시 확인할 수 있다.
- 선택 경로는 Google Maps 경로 링크와 일정 저장 기능으로 연결된다.

발표용 설명:

> 최종 화면에서는 챗봇의 답변을 단순 텍스트로 끝내지 않고, 경로 후보 비교, 상세 타임라인, 최근 경로 저장, 일정 저장까지 이어지도록 구현했습니다.

## 7-2. TMAP POI 기반 편의시설 구현

### 기능 목표

사용자가 현재 위치 버튼을 누르면, 현재 위치 기준 반경 3km 안의 은행, ATM, 편의점, 화장실을 조회한다.

### 구현 방식

- FE에서 Expo Location으로 현재 좌표 획득
- Spring Boot `/api/facilities/nearby` 호출
- TMAP 주변 POI API 호출
- 카테고리, 거리, 주소, 위경도 정규화
- 최초 10개 조회 후, 스크롤 시 5개씩 추가 로딩

### API 파라미터

```text
GET /api/facilities/nearby
  lat
  lng
  radius=3
  offset=0
  size=10
  categories=은행,ATM,편의점,화장실
```

### 구현 포인트

- DB를 새로 만들지 않고 기존 TMAP POI API를 재사용했다.
- 과도한 요청을 막기 위해 페이지네이션을 적용했다.
- POI 응답의 도로명 주소, 지번 주소, 좌표를 프론트에서 바로 사용할 수 있게 정규화했다.
- 시설 카드를 누르면 Google Maps 웹/앱으로 연결되도록 했다.

### 발표 강조 문장

> 편의시설 기능은 별도 DB를 구축하지 않고 TMAP POI를 재사용했습니다. 현재 위치, 반경, 카테고리, 페이지네이션만 조합해 여행 중 바로 필요한 주변 정보를 가볍게 조회할 수 있게 했습니다.

### 기술 구현 상세

편의시설 기능은 교통 챗봇과 달리 Gemini를 사용하지 않는다. 사용자의 현재 위치 좌표를 기준으로 TMAP 주변 POI API를 호출하는 구조다.

#### 1단계: 현재 위치 획득

관련 파일:

- `FacilitiesScreen.tsx`
- `useNearbyFacilities.ts`

구현 방식:

- 사용자가 오른쪽 하단 현재 위치 버튼을 누른다.
- Expo Location으로 위치 권한을 요청한다.
- 권한이 허용되면 현재 위도/경도를 가져온다.
- 가져온 좌표를 기준으로 편의시설 API를 호출한다.

#### 2단계: 백엔드 API 요청

관련 파일:

- `FacilityController.java`
- `FacilityService.java`

API:

```text
GET /api/facilities/nearby
```

요청 예시:

```text
/api/facilities/nearby?lat=37.5&lng=127.0&radius=3&offset=0&size=10&categories=은행,ATM,편의점,화장실
```

구현 방식:

- `radius`는 1~10km 범위로 제한한다.
- `size`는 1~20개 범위로 제한해 과도한 요청을 방지한다.
- 기본 카테고리는 은행, ATM, 편의점, 화장실이다.
- FE에서는 최초 10개를 받고, 스크롤 시 5개씩 추가 요청한다.

#### 3단계: TMAP 주변 POI 호출

관련 파일:

- `TmapClient.java`

TMAP 요청 값:

```text
centerLat
centerLon
radius
categories
page
count
sort=distance
reqCoordType=WGS84GEO
resCoordType=WGS84GEO
```

구현 방식:

- TMAP `/tmap/pois/search/around` API를 사용한다.
- 결과는 거리순으로 정렬한다.
- 응답의 도로명 주소, 지번 주소, 위도, 경도, 거리 값을 정규화한다.
- TMAP 응답 카테고리명이 불명확한 경우 POI 이름과 업종명을 기준으로 은행/ATM/편의점/화장실을 재분류한다.

#### 4단계: 프론트 표시와 외부 지도 연결

관련 파일:

- `FacilityCard.tsx`
- `FacilityCategoryTabs.tsx`

구현 방식:

- 상단 카테고리 탭으로 결과를 다시 조회한다.
- 각 카드에는 시설명, 카테고리, 주소, 거리, 외부 링크 아이콘을 표시한다.
- 시설을 누르면 위도/경도 기반 Google Maps 링크를 연다.
- 화장실처럼 POI 이름에 일반 카테고리명이 붙어 검색 정확도가 떨어질 수 있는 경우, 지도 링크는 좌표 중심으로 생성해 정확도를 높였다.

발표용 설명:

> 편의시설 기능의 핵심은 POI 데이터를 앱에서 사용하기 좋은 형태로 정규화한 것입니다. TMAP 응답을 그대로 보여주는 것이 아니라 주소 우선순위, 카테고리 매핑, 거리 계산, 페이지네이션을 적용했습니다.

## 7-3. 행사/축제 DB 연동

### 기능 목표

정적 더미 데이터가 아니라 백엔드 DB에 저장된 행사 데이터를 앱에서 동적으로 조회한다.

### 구현 방식

- `/api/festival/festivalList`: 현재 날짜 기준 N개월 이내 행사 조회
- `/api/festival/festivalDetail`: contentId 기반 상세 조회
- FE에서 지역별 필터, 진행 상태 필터 적용
- 상세 화면에서 공식 홈페이지 연결
- 로그인 사용자는 관심 행사를 저장하거나 일정에 등록 가능

### 최종 필터 구성

- 지역별 필터
- 전체
- 진행 중
- 곧 시작: D-7 기준
- 종료 임박: D-3 기준

### 발표 강조 문장

> 행사 데이터는 프론트의 더미 데이터가 아니라 RDS에 저장된 행사 DB를 API로 받아오도록 변경했습니다. 사용자는 행사 목록, 상세 정보, 홈페이지, 저장, 캘린더 등록까지 하나의 흐름으로 사용할 수 있습니다.

### 기술 구현 상세

행사 기능은 최종 앱에서 더미 데이터 의존을 제거하고 RDS 기반 API로 전환한 부분이다.

#### 1단계: 행사 목록 조회

관련 파일:

- `FestivalController.java`
- `FestivalService.java`
- `FestivalInfoRepository.java`
- `src/app/api/festival.ts`
- `useFestivalEvents.ts`

API:

```text
GET /api/festival/festivalList?months=3
```

구현 방식:

- 백엔드는 현재 날짜를 기준으로 `now ~ now + months` 범위의 행사 데이터를 조회한다.
- DB의 `festival_info` 테이블에서 contentId, 제목, 이미지, 시작일, 종료일, 주소를 가져온다.
- FE는 응답을 `FestivalEventItem`으로 변환한다.
- 지역 필터와 상태 필터는 FE에서 계산한다.

#### 2단계: 행사 상세 조회

API:

```text
POST /api/festival/festivalDetail?contentId={contentId}
```

구현 방식:

- 목록에서 선택한 행사의 `contentId`를 기준으로 상세 정보를 조회한다.
- 상세 정보에는 주소, 우편번호, 기간, 이미지, 전화번호, 요금, 홈페이지, 개요가 포함된다.
- 홈페이지 URL은 프론트에서 정리한 뒤 외부 브라우저로 연다.

#### 3단계: 행사 저장 및 일정 등록

관련 파일:

- `ScheduleController.java`
- `ScheduleService.java`
- `SavedFestival.java`
- `ScheduleItem.java`
- `EventDetailScreen.tsx`
- `SavedEventsScreen.tsx`

구현 방식:

- 로그인 사용자는 행사 상세 화면에서 관심 행사를 저장할 수 있다.
- 저장 데이터는 `saved_festival` 테이블에 사용자와 행사 관계로 저장된다.
- 일정 등록 시 `schedule_item` 테이블에 날짜, 타입, 제목, 메모, 행사 contentId가 저장된다.
- 캘린더와 내 일정 화면은 같은 schedule API를 사용해 데이터를 조회한다.

발표용 설명:

> 행사 기능은 단순 목록 조회에서 끝나지 않습니다. 행사 상세, 저장, 캘린더 등록이 같은 DB 흐름으로 이어지며, JWT 인증 사용자 기준으로 개인화됩니다.

## 7-4. 음식 알레르기 기반 안전 음식 추천

### 기능 목표

사용자가 피해야 하는 알레르기 성분을 선택하면, 해당 성분이 포함되지 않은 음식 목록을 DB에서 필터링해 제공한다.

### 구현 방식

- FE에서 알레르기 체크박스 선택
- `/api/food/safeFoodList` 호출
- Spring Boot에서 선택 알레르기 목록 기준 DB 필터링
- 응답은 카테고리별 음식 목록과 음식별 성분 정보 포함
- FE는 음식명, 분류, 포함 성분을 간결하게 표시

### 지원 알레르기 예시

- 난류
- 우유
- 밀
- 메밀
- 견과류
- 대두
- 갑각류
- 해산물
- 돼지고기
- 쇠고기
- 닭고기

### 발표 강조 문장

> 음식 기능은 단순 추천이 아니라 사용자가 선택한 알레르기 조건을 서버에서 필터링합니다. 또한 음식별 성분 정보를 같이 내려주기 때문에, 외국인 사용자가 한국 음식 선택 시 위험 성분을 확인할 수 있습니다.

### 기술 구현 상세

음식 기능은 처음에는 CSV/정적 데이터 중심으로 동작했지만, 최종본에서는 DB 기반 필터링과 성분 표시를 반영했다.

#### 1단계: 프론트 알레르기 선택

관련 파일:

- `FoodScreenAllergy.tsx`
- `AllergyFilterGrid.tsx`
- `useSafeFoods.ts`

구현 방식:

- 사용자는 난류, 우유, 밀, 메밀, 견과류 등 피해야 할 성분을 선택한다.
- 선택된 배열을 `selectedAllergies`로 서버에 전송한다.
- 검색어가 있으면 FE에서 음식명, 카테고리, 태그, 성분을 기준으로 추가 필터링한다.

#### 2단계: 백엔드 안전 음식 필터링

관련 파일:

- `FoodController.java`
- `FoodService.java`
- `FoodInfoRepository.java`

API:

```text
POST /api/food/safeFoodList
```

요청:

```json
{
  "selectedAllergies": ["갑각류", "견과류"]
}
```

구현 방식:

- 선택 알레르기가 없으면 전체 음식 목록을 반환한다.
- 선택 알레르기가 있으면 해당 알레르기 성분이 포함된 음식을 제외한다.
- 필터링은 서버 DB 기준으로 수행한다.

#### 3단계: 응답 DTO 확장

관련 파일:

- `FoodResponse.java`
- `FoodCard.tsx`

응답 구조:

```json
{
  "categories": [
    {
      "category": "볶음",
      "foodNames": ["오징어불고기"],
      "foods": [
        {
          "foodId": 1,
          "foodName": "오징어불고기",
          "category": "볶음",
          "ingredients": ["오징어", "양파", "고추장"],
          "ingredientNm": "오징어, 양파, 고추장"
        }
      ]
    }
  ]
}
```

구현 방식:

- 기존 `foodNames`만 내려주던 구조에 `foods` 배열을 추가했다.
- 각 음식에 `ingredients`와 `ingredientNm`을 포함해 FE가 성분을 표시할 수 있게 했다.
- FE는 `categories[].foods`를 우선 사용하고, 구형 응답 호환을 위해 `foodNames`도 처리한다.

발표용 설명:

> 최종 구현에서는 음식명만 보여주는 것이 아니라 음식별 성분까지 내려주도록 DTO를 확장했습니다. 덕분에 사용자는 왜 이 음식이 조건에 맞는지 더 명확하게 확인할 수 있습니다.

## 7-5. 저장 행사 및 캘린더/일정 구현

### 기능 목표

사용자가 앱에서 찾은 정보를 일회성으로 보는 데서 끝내지 않고, 개인 여행 일정으로 저장할 수 있게 한다.

### 구현 API

```text
GET    /api/saved-festivals
POST   /api/saved-festivals/{contentId}
DELETE /api/saved-festivals/{contentId}

GET    /api/schedules?date=YYYY-MM-DD
GET    /api/schedules/month?year=YYYY&month=M
POST   /api/schedules/items
PUT    /api/schedules/items/{itemId}
DELETE /api/schedules/items/{itemId}
```

### 구현 포인트

- JWT 인증 사용자 기준으로 저장 데이터 분리
- 저장한 행사를 마이페이지에서 확인
- 행사 상세에서 바로 일정 등록
- 캘린더에서 월별 일정 표시
- 내 일정 화면에서 메모/일정 추가, 수정, 삭제 가능
- 교통 경로도 일정 형태로 저장 가능하도록 확장

### 발표 강조 문장

> 최종 앱에서는 행사 탐색, 경로 추천, 일정 저장이 분리된 기능이 아니라 사용자 데이터로 이어집니다. JWT 인증을 기준으로 저장 행사와 일정이 개인별로 관리됩니다.

### 기술 구현 상세

일정 기능은 최종 단계에서 사용자별 저장 데이터를 실제 DB에 연결한 부분이다.

#### 1단계: JWT 기반 사용자 식별

구현 방식:

- 로그인 성공 시 서버가 JWT 문자열을 반환한다.
- FE는 토큰을 저장하고 API 요청 시 Authorization 헤더에 포함한다.
- Spring Security는 토큰에서 사용자 식별 값을 꺼낸다.
- 저장 행사와 일정 API는 `Authentication authentication`을 통해 현재 사용자를 확인한다.

발표용 설명:

> 일정과 저장 기능은 로그인 사용자별 데이터이기 때문에 JWT 인증 흐름과 직접 연결됩니다. 같은 API를 호출하더라도 토큰에 따라 다른 사용자의 데이터가 조회됩니다.

#### 2단계: 저장 행사

DB 구조:

```text
saved_festival
  saved_festival_id
  user_id
  festival_content_id
  created_at
```

구현 방식:

- 사용자가 행사 상세에서 저장을 누르면 `POST /api/saved-festivals/{contentId}`를 호출한다.
- 서버는 현재 사용자와 행사 contentId의 관계를 저장한다.
- 이미 저장된 행사는 중복 저장하지 않고 기존 데이터를 반환한다.
- 삭제는 `DELETE /api/saved-festivals/{contentId}`로 처리한다.

#### 3단계: 캘린더/내 일정

DB 구조:

```text
schedule_item
  item_id
  user_id
  schedule_date
  type
  title
  memo
  start_time
  end_time
  festival_content_id
  place_name
  address
  lat
  lng
  sort_order
  created_at
  updated_at
```

구현 방식:

- 월별 캘린더는 `/api/schedules/month`로 데이터를 조회한다.
- 특정 날짜 상세는 `/api/schedules?date=YYYY-MM-DD`로 조회한다.
- 내 일정에서 메모나 일정을 추가하면 `/api/schedules/items`로 저장한다.
- 수정은 `PUT`, 삭제는 `DELETE`로 처리한다.
- 행사, 메모, 장소, 교통 경로를 모두 `type`으로 구분해 같은 일정 테이블에서 관리할 수 있게 했다.

발표용 설명:

> 일정 테이블은 단순 메모뿐 아니라 행사, 장소, 교통 경로까지 담을 수 있도록 타입 기반으로 설계했습니다. 이를 통해 앱의 여러 기능이 최종적으로 개인 일정으로 연결됩니다.

## 7-6. 프론트엔드 API 연결 구조

최종 앱에서는 API 호출을 화면 컴포넌트 안에 직접 흩뿌리지 않고, `src/app/api`와 custom hook으로 분리했다.

### 구조

```text
src/app/api
  auth.ts
  client.ts
  festival.ts
  food.ts
  facilities.ts
  transport.ts
  userContent.ts

src/app/hooks
  useFestivalEvents.ts
  useHomeFestivalBanners.ts
  useNearbyFacilities.ts
  useSafeFoods.ts
  useTransportChat.ts
```

### 구현 포인트

- `api/client.ts`: 공통 API 요청, base URL, JWT 헤더 처리
- `transport.ts`: 교통 후보/경로 응답 normalize
- `food.ts`: 카테고리/음식/성분 응답 normalize
- `facilities.ts`: 편의시설 카테고리와 주변 검색 응답 처리
- `userContent.ts`: 저장 행사와 일정 CRUD API
- hook 계층: 화면 로딩 상태, 에러 상태, 데이터 변환 상태 관리

발표용 설명:

> 프론트에서는 API 요청, 데이터 정규화, 화면 상태를 분리했습니다. 이 구조 덕분에 백엔드 응답 필드명이 조금 달라도 화면이 깨지지 않도록 보완할 수 있었고, 각 화면은 UI 표현에 집중할 수 있었습니다.

## 7-7. 더미 데이터 제거와 실제 데이터 전환

최종 구현에서 중요한 작업은 화면에 박혀 있던 더미 데이터를 실제 API 데이터로 교체한 것이다.

### 전환된 부분

| 기능 | 기존 상태 | 최종 상태 |
| --- | --- | --- |
| 행사 목록 | 정적 카드/더미 데이터 | RDS 행사 DB API |
| 음식 목록 | CSV/정적 fallback 중심 | DB 알레르기 필터 API |
| 캘린더 | 고정 더미 일정 | 사용자별 schedule API |
| 내 일정 | 화면 내부 임시 데이터 | DB 저장/수정/삭제 |
| 교통 경로 | 현재 세션 결과만 표시 | 최근 경로 임시 저장 + 일정 저장 |
| 편의시설 | UI 중심 | TMAP POI 실시간 조회 |

발표용 설명:

> 최종 단계에서는 앱이 그럴듯하게 보이는 더미 화면을 넘어서, 실제 서버와 DB, 외부 API 응답으로 동작하도록 전환했습니다. 특히 행사, 음식, 일정은 DB 기반으로 바뀌었고, 교통과 편의시설은 TMAP 실데이터와 연결됐습니다.

## 8. 구현 결과

### 최종 앱에서 확인 가능한 결과

- 로그인/회원가입 후 JWT 인증 기반 API 호출
- 홈 화면에서 행사, 편의시설, 교통, 음식, 결제, 긴급 상황 접근
- 행사 DB 조회 및 상세 페이지 이동
- 행사 저장, 저장 목록 확인, 캘린더 등록
- 음식 알레르기 선택 후 안전 음식 목록 및 성분 확인
- 현재 위치 기반 주변 편의시설 조회
- 시설 선택 시 Google Maps 연결
- 교통 챗봇 자연어 입력
- 출발지/목적지 후보 선택
- 추천 경로 3개 비교
- 선택 경로 상세 타임라인 표시
- 선택 경로 최근 기록 및 일정 저장

### 발표에서 보여주면 좋은 시연 순서

1. 로그인
2. 행사 탭에서 DB 행사 목록 조회
3. 행사 상세에서 저장 및 일정 등록
4. 캘린더에서 등록된 일정 확인
5. 음식 탭에서 알레르기 선택 후 필터링 결과 확인
6. 편의시설에서 현재 위치 기반 POI 조회
7. 교통 챗봇에 자연어 입력
8. POI 후보 선택 후 경로 3개 비교
9. 상세 경로와 최근 경로 저장 확인

## 9. 기대 효과

### 사용자 측면

- 한국 여행 중 필요한 기능을 앱 하나에서 사용할 수 있다.
- 언어 장벽이 있어도 자연어 챗봇으로 교통 경로를 물어볼 수 있다.
- 알레르기 성분을 고려해 안전한 음식을 선택할 수 있다.
- 주변 편의시설을 현재 위치 기준으로 빠르게 찾을 수 있다.
- 행사 탐색 결과를 일정으로 저장해 여행 계획을 유지할 수 있다.

### 기술 측면

- AI 응답을 실제 TMAP 데이터와 결합해 신뢰도를 높였다.
- Spring Boot와 FastAPI를 역할별로 분리해 유지보수성을 확보했다.
- RDS 기반으로 사용자별 저장/일정 기능을 확장할 수 있다.
- POI, 대중교통, 행사, 음식 데이터를 하나의 앱 UX로 통합했다.

## 10. 한계점 및 향후 개선 방향

### 현재 한계점

- 교통 경로는 대중교통 중심이며, 도보/택시/자전거 등 멀티모달 확장 여지가 있다.
- Gemini 추천 사유는 모델 응답 품질에 영향을 받을 수 있어 프롬프트 및 검증 로직 고도화가 필요하다.
- 행사 데이터는 DB에 저장된 범위 안에서만 제공되므로 데이터 최신화 파이프라인이 필요하다.
- 음식 알레르기 정보는 DB 정확도에 의존하므로 성분 데이터 검수와 다국어 표시가 필요하다.
- 현재 위치 기반 기능은 위치 권한 거부, 네트워크 오류, API 제한에 대한 사용자 안내를 더 개선할 수 있다.
- 다국어 UI는 최종 외국인 대상 서비스 관점에서 더 보강할 필요가 있다.

### 향후 개선 방향

- 교통 경로 기록 조회 API 추가
- 사용자가 선택한 경로를 서버 DB에 저장하고 다시 불러오기
- 축제/행사 데이터 자동 업데이트 배치 구축
- 음식 성분 다국어 번역 및 위험 성분 강조
- 장소/행사/음식 즐겨찾기 통합 마이페이지
- 위치 기반 추천: 근처 행사, 근처 음식, 근처 편의시설 통합 추천
- 오프라인 상황 대비 최근 조회 데이터 캐싱
- 외국인 사용자 대상 영어/중국어/일본어 UI 확장

## 최종 PPT 슬라이드 구성 추천

기존 43장 발표는 최종 발표용으로는 다소 길다. 18~22장 정도가 적절하다.

### 1. Title

제목: 가자GO 최종 기술 발표  
부제: 방한 외국인을 위한 AI 기반 한국 여행 지원 앱

### 2. 프로젝트 개요

- 대상: 한국을 방문한 외국인 여행자
- 목표: 행사, 교통, 음식, 편의시설, 일정을 하나의 앱 흐름으로 연결
- 핵심 기술: Gemini, TMAP API, Spring Boot, FastAPI, RDS

### 3. 기존 서비스 분석

- 지도/관광/음식 서비스가 기능별로 분산
- 일반 챗봇은 실제 장소/교통 데이터 검증 부족
- 가자GO는 여행 중 필요한 실행 정보를 통합 제공

### 4. 최종 시스템 아키텍처

시각 자료 추천:

```text
Expo App -> Spring Boot -> RDS
                   |-> TMAP POI
                   |-> FastAPI -> Gemini + TMAP Transit
```

### 5. API 연동 현황

표 추천:

| 기능 | API | 데이터 출처 |
| --- | --- | --- |
| 회원 | /api/member/login | RDS user |
| 행사 | /api/festival/* | RDS festival_info |
| 음식 | /api/food/safeFoodList | RDS food/allergy |
| 편의시설 | /api/facilities/nearby | TMAP POI |
| 교통 | /api/transport/* | Gemini + TMAP |
| 일정 | /api/schedules/* | RDS schedule_item |

### 6. 교통 챗봇 전체 흐름

- 자연어 입력
- Gemini 추출
- TMAP POI 후보 검증
- 사용자 장소 확정
- TMAP 대중교통 경로 조회
- Gemini 경로 비교
- 앱 상세 경로 표시

### 7. Gemini 적용 지점

- 사용자 문장에서 출발지/목적지/제약 조건 추출
- 경로 후보 3개의 시간/환승/도보/요금 비교
- 사용자 제약 조건에 맞는 우선순위 생성

### 8. TMAP POI 적용 지점

- 교통 챗봇 장소 후보 검증
- 주변 편의시설 검색
- 주소, 좌표, 거리 정규화

### 9. 편의시설 구현 결과

- 현재 위치 기준 반경 3km
- 은행/ATM/편의점/화장실
- 최초 10개 + 추가 5개 로딩
- Google Maps 외부 연결

### 10. 행사 DB 구현 결과

- 동적 행사 목록
- 지역/진행 상태 필터
- 상세 페이지
- 공식 홈페이지 연결
- 저장 및 일정 등록

### 11. 음식 알레르기 구현 결과

- 알레르기 선택
- 서버 DB 필터링
- 음식별 성분 표시
- CSV/더미 데이터 의존 제거

### 12. 사용자 일정/저장 구현 결과

- 저장 행사
- 캘린더 월별 일정
- 내 일정 메모
- 일정 수정/삭제
- JWT 사용자별 데이터 분리

### 13. 최종 앱 화면 흐름

시연 흐름을 캡처 이미지로 배치:

홈 -> 행사 -> 저장 -> 캘린더 -> 음식 -> 편의시설 -> 교통 챗봇

### 14. 기술적으로 해결한 문제

- API 엔드포인트 불일치 수정
- JWT 인증 헤더 유지
- FE/BA CORS 및 네트워크 주소 정리
- TMAP/Gemini 키 환경변수 매핑
- 더미 데이터 제거 및 DB 연동
- 음식 성분 응답 DTO 확장

### 15. 검증 결과

- Expo 앱 테스트
- Swagger API 테스트
- Docker 기반 서버 실행
- FE typecheck 통과
- Backend bootJar 통과
- 실제 TMAP/Gemini/RDS 응답 확인

### 16. 기대 효과

- 방한 외국인의 이동/음식/행사 탐색 부담 감소
- AI와 실데이터 결합으로 신뢰도 높은 교통 추천
- 저장/일정 기능으로 여행 계획 연속성 확보

### 17. 한계점

- 다국어 UI 보강 필요
- 행사/음식 데이터 최신화 자동화 필요
- 교통 기록 서버 조회 기능 추가 필요
- 외부 API 장애/쿼터 대응 필요

### 18. 향후 개선 방향

- 경로 기록 DB 조회
- 다국어 번역
- 근처 행사/음식 통합 추천
- 여행 일정 자동 생성
- 오프라인 캐싱

### 19. 마무리

핵심 결론:

> 가자GO는 단순 여행 정보 앱이 아니라, AI 자연어 이해와 TMAP 실데이터, RDS 기반 개인 일정을 연결한 방한 외국인용 여행 실행 보조 앱입니다.

## 기존 PPT에서 줄이면 좋은 부분

줄이거나 한 장으로 압축:

- Expo React Native 특징
- TypeScript 선정 이유
- Spring 특징
- Docker/AWS 일반 설명
- Structured Output 개념 설명이 반복되는 장표
- Function Calling 일반 개념 설명

살리되 최종 구현 중심으로 바꿀 부분:

- 전체 아키텍처
- TMAP POI 필요 이유
- POI 장소 검증 과정
- 대중교통 경로 후보 조회
- Gemini 챗봇
- 전체 시퀀스
- 전체 구현화
- 향후 계획

## 발표 대본 요약

### 시작

가자GO는 한국을 방문한 외국인이 여행 중 가장 자주 겪는 문제인 이동, 음식, 행사 탐색, 주변 편의시설 찾기, 일정 관리를 하나의 앱에서 해결하기 위한 프로젝트입니다.

### 기술 핵심

가장 중요한 구현은 교통 챗봇입니다. 사용자가 자연어로 질문하면 Gemini가 출발지, 목적지, 제약 조건을 구조화하고, TMAP POI API가 실제 장소 후보를 검증합니다. 사용자가 후보를 선택하면 TMAP 대중교통 API로 경로 후보를 가져오고, Gemini가 시간, 환승, 도보 거리, 요금, 사용자 조건을 비교해 우선순위를 생성합니다.

### 앱 완성도

최종 앱에서는 교통뿐 아니라 행사, 음식, 편의시설, 일정 기능도 DB/API 기반으로 연결했습니다. 행사 데이터는 RDS에서 조회하고, 음식은 알레르기 조건에 따라 서버에서 필터링하며, 편의시설은 현재 위치 기반 TMAP POI로 조회합니다. 저장한 행사와 메모는 JWT 인증 사용자 기준으로 캘린더와 내 일정에 저장됩니다.

### 마무리

이 프로젝트의 핵심은 AI를 단독으로 쓰는 것이 아니라, 실제 위치/교통/DB 데이터와 결합해 사용자가 바로 행동할 수 있는 결과로 만드는 것입니다. 향후에는 다국어 지원, 경로 기록 조회, 데이터 자동 업데이트를 통해 실제 방한 외국인 서비스에 더 가까운 형태로 확장할 수 있습니다.
